PORT=3000
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_USERNAME=your_bot_username_here
ALLOW_GUESTS=false
# Optional: set to your deployed host (no path). Used to redirect if the current host doesn't match.
# Examples:
# APP_HOST=realtime-tictactoe-0f8i.onrender.com
# APP_HOST=https://realtime-tictactoe-0f8i.onrender.com
APP_HOST=realtime-tictactoe-0f8i.onrender.com

# Session signing secret for login tokens (JWT)
# If unset, the server will generate a temporary secret and logins will NOT survive server restarts.
SESSION_SECRET=change_me_to_a_long_random_string

# MongoDB
# Defaults used by the server if unset:
# MONGODB_URI=mongodb://127.0.0.1:27017
# MONGODB_DB_NAME=tictactoe
MONGODB_URI=mongodb+srv://mgtube75_db_user:pJN7QNxgg8WdsMcQ@cluster0.deuu6tq.mongodb.net
MONGODB_DB_NAME=tictactoeGame
